### IDE ###
.idea/
*.iws
*.iml
*.ipr
.project
.classpath
.settings/
.factorypath
.vscode/
bin/

### OS ###
.DS_Store
Thumbs.db

### Generated by the Site workflow ###
# Rebuilt from this branch on every deployment, so it is derived state and is
# deliberately not tracked here. Ignoring it (rather than only deleting it) keeps
# the old single-job workflow's `git add -A` from committing it straight back.
index.html

